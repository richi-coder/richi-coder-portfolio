---
import "./styles/title.css"
const { id, title, preTitle, titleClass } = Astro.props;
---
    <div id={id} class={`title text-white w-full text-center text-6xl sm:text-9xl z-40 ${titleClass}`}>
      
            <div class="logoTitle">
            {preTitle}<span class="text-gradient">{title}</span>
            </div>
            
    </div>
